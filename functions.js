// functions.js

const fs = require('fs').promises;
const path = require('path');

function mdLinks(filePath) {
  return new Promise((resolve, reject) => {
    // Implementa aquí la lógica de mdLinks
  });
}

module.exports = {
  mdLinks,
};
